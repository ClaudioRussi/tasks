<% content_for :title, 'Create a group'%>
<%= render partial: 'layouts/errors', locals:{subject: @group}%>
<%= form_for [@project,@group] do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.submit 'Create group' %>
<%end%>
